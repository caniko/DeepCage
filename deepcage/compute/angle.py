from numpy.linalg import norm
import numpy as np

from .utils import *


def non_sessile_angle_change(p1, p2):
    """
    Parameters
    ----------
    p1 : numpy.array-like
    p2 : numpy.array-like
    """

    line_unit_v = np.where(p1 - p2

    baseline = np.median(line_unit_v)
